services:
  stylemuse:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./.env:/app/.env:ro
      - ./authors:/app/authors
      - ./logs:/app/logs
    env_file:
      - .env
    environment:
      FLASK_HOST: 0.0.0.0
      FLASK_DEBUG: "0"
    restart: unless-stopped
